# MOB-002: Flutter UIScene lifecycle migration completion

## Status

- Task ID: MOB-002
- Status: Completed locally and committed, awaiting review and integration
- Agent: Codex
- Worktree: `/Users/tanyasmritivictorcharles/dev/main-inventory-app`
- Branch: `mobile/flutter-uiscene-migration`
- Commit: `335a3faaee5284d451e41dedd00ae98110b28af8`

## Objective

Finish the existing Flutter UIScene lifecycle migration without changing unrelated
mobile behavior, validate a signed iOS build and physical launch, and record the
release state before MOB-004 Phase A begins.

## Work completed

- Added the RunnerTests CocoaPods target and regenerated one clean CocoaPods test
  integration in the Xcode project.
- Removed duplicate RunnerTests CocoaPods references created by combining the earlier
  manual Xcode integration with generated pod integration.
- Preserved the existing UIScene manifest, `FlutterSceneDelegate`, app entitlements,
  signing team, bundle identifier, ShareExtension target, and URL handling.
- Kept launch notification capture and moved Flutter plugin and push-channel setup to
  `didInitializeImplicitFlutterEngine`, which is the UIScene-compatible lifecycle hook.
- Built a signed release, installed it on Tanya's physical iPhone, launched it, and
  confirmed the Runner process remained active.

## Files changed

- `mobile/ios/Podfile`
- `mobile/ios/Podfile.lock`
- `mobile/ios/Runner.xcodeproj/project.pbxproj`
- `mobile/ios/Runner/AppDelegate.swift`

## API and database changes

None.

## Tests and checks performed

- `pod install`: passed, 14 dependencies and 28 pods installed.
- `plutil -lint` on Runner and ShareExtension Info.plist and entitlements: passed.
- `flutter analyze`: passed with no issues.
- `flutter test`: passed, 20 tests.
- RunnerTests simulator test: passed.
- Signed release iOS build using `.env`: passed.
- Physical device install: passed.
- Physical device launch and process check: passed.
- `git diff --check`: passed before commit.
- Final diff review confirmed only the four scoped UIScene files changed.

## Decisions made

- Use CocoaPods-generated RunnerTests integration instead of retaining duplicate
  manual and generated project references.
- Keep push registration on the implicit Flutter engine lifecycle so notification
  behavior remains available under UIScene.
- Do not upload to TestFlight in this task. The user requested completion and a local
  commit, not a push or release.

## Remaining work

- Review and integrate commit `335a3fa` into the agreed mobile base.
- Confirm TestFlight availability only as part of an explicitly authorized release.
- After integration, create a clean worktree for MOB-004 Phase A.

## Blockers

- The commit is local and has not been pushed or integrated.
- Current App Store Connect and TestFlight availability was not checked in this task.
- Flutter native asset caches can retain simulator architecture after simulator tests.
  A clean, device-first generated build produced the valid signed physical build.

## Exact next step

Review and integrate commit `335a3fa` into the agreed mobile base. Then create a clean
mobile feature worktree from that base for MOB-004 Phase A. Do not begin Phase A in
the UIScene worktree.
